import { MouseTracker } from './mouse-tracker';

describe('MouseTracker', () => {
  it('should create an instance', () => {
    const directive = new MouseTracker();
    expect(directive).toBeTruthy();
  });
});
